<%- include ('../partials/head') %>
<%- include ('../partials/nav') %>

<div class="bg-gray-50 min-h-screen py-10 px-4">
  <div class="max-w-6xl mx-auto">
    
    <div class="bg-gray-50 min-h-screen py-10 px-4">
  <div class="max-w-6xl mx-auto">
    
    <div class="flex flex-col lg:flex-row justify-between items-start lg:items-end mb-6 gap-4">
      
      <div>
        <h1 class="text-3xl font-bold text-gray-800">Guest List</h1>
        <p class="text-gray-500 text-sm mt-1">
          Property: <span class="font-bold text-red-700"><%= home.housename %></span>
        </p>
      </div>

      <div class="flex flex-col sm:flex-row gap-3 w-full lg:w-auto">
        
        <form action="/host/report/<%= home._id %>" method="GET" class="flex items-center gap-2 bg-white p-2 rounded-lg border border-gray-300 shadow-sm w-full sm:w-auto">
          
          <div class="flex flex-col">
            <label class="text-[10px] font-bold text-gray-400 uppercase ml-1">From</label>
            <input type="date" name="startDate" required class="text-xs border border-gray-200 rounded px-2 py-1 outline-none focus:border-red-500 text-gray-700">
          </div>

          <div class="flex flex-col">
            <label class="text-[10px] font-bold text-gray-400 uppercase ml-1">To</label>
            <input type="date" name="endDate" required class="text-xs border border-gray-200 rounded px-2 py-1 outline-none focus:border-red-500 text-gray-700">
          </div>

          <button type="submit" class="mt-auto bg-red-600 hover:bg-red-700 text-white text-xs font-bold py-1.5 px-4 rounded shadow-md transition flex items-center gap-2 h-[26px] whitespace-nowrap">
            <i class="fas fa-file-invoice"></i> Generate
          </button>
        </form>

        <a href="/host-home-list" class="bg-white border border-gray-300 text-gray-700 hover:bg-gray-50 text-xs font-bold py-3 px-4 rounded-lg shadow-sm flex items-center justify-center gap-2 transition">
          <i class="fas fa-arrow-left"></i> Back
        </a>

      </div>
    </div>

    <div class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
      <% if (bookings.length > 0) { %>
        <div class="overflow-x-auto">
          <table class="w-full text-left border-collapse">
            <thead>
              <tr class="bg-red-50 text-red-800 text-sm uppercase tracking-wider">
                <th class="p-4 font-bold border-b border-red-100">Guest Name</th>
                <th class="p-4 font-bold border-b border-red-100">Dates</th>
                <th class="p-4 font-bold border-b border-red-100">Contact</th>
                <th class="p-4 font-bold border-b border-red-100">Guests</th>
                <th class="p-4 font-bold border-b border-red-100">Revenue</th>
                <th class="p-4 font-bold border-b border-red-100">Status</th>
              </tr>
            </thead>
            <tbody class="text-gray-700 text-sm">
              <% bookings.forEach(booking => { %>
                <tr class="hover:bg-gray-50 transition border-b border-gray-100 last:border-0">
                  
                  <td class="p-4 font-semibold">
                    <%= booking.customername %>
                  </td>

                  <td class="p-4">
                    <div class="flex flex-col">
                      <span class="font-bold"><%= new Date(booking.startDate).toLocaleDateString() %></span>
                      <span class="text-xs text-gray-400">to</span>
                      <span class="font-bold"><%= new Date(booking.endDate).toLocaleDateString() %></span>
                    </div>
                  </td>

                  <td class="p-4">
                    <div class="text-xs">
                      <p><i class="fas fa-envelope text-gray-400 mr-1"></i> <%= booking.customeremail %></p>
                      <p class="mt-1"><i class="fas fa-phone text-gray-400 mr-1"></i> <%= booking.customernumber %></p>
                    </div>
                  </td>

                  <td class="p-4 text-center">
                    <%= booking.guestCount %>
                  </td>

                  <td class="p-4 font-bold text-green-600">
                    ₹<%= booking.totalPrice %>
                  </td>

                  <td class="p-4">
                    <span class="px-2 py-1 rounded-full text-xs font-bold uppercase
                      <%= booking.status === 'confirmed' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-600' %>">
                      <%= booking.status %>
                    </span>
                  </td>

                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div class="text-center py-16">
          <div class="bg-gray-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
            <i class="fas fa-clipboard-list text-2xl text-gray-400"></i>
          </div>
          <h3 class="text-xl font-bold text-gray-800">No Bookings Yet</h3>
          <p class="text-gray-500 mt-2">This property has no upcoming events scheduled.</p>
        </div>
      <% } %>
    </div>

  </div>
</div>