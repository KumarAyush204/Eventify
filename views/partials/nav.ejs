<nav class="w-full bg-red-700 shadow-md">
  <div class="max-w-7xl mx-auto px-6 py-3 flex items-center justify-between">
    
    <!-- Logo / Home -->
    <a href="/" class="text-white text-lg font-bold tracking-wide hover:text-red-200 transition">
      Eventify
    </a>

    <!-- Navigation Links -->
    <div class="flex items-center space-x-6">
      <% if (isLoggedIn) { %>
        <% if (user && user.usertype==='guest') { %>
        
        <a href="/bookings" class="text-white hover:text-red-200 transition font-medium">Bookings</a>
        <a href="/favourites" class="text-white hover:text-red-200 transition font-medium">Favourites</a>
        <!-- <a href="/add-home" class="text-white hover:text-red-200 transition font-medium">Reserved</a> -->
      <% }else{ %>
        
        <a href="/add-home" class="text-white hover:text-red-200 transition font-medium">Add Venue</a>
        <a href="/home-list" class="text-white hover:text-red-200 transition font-medium">
      Your Venues
    </a>
    <% } %>
<% } %>

      <% if (!isLoggedIn) { %>
        <a href="/login" class="text-white hover:text-red-200 transition font-medium">Login</a>
        <a href="/signup" class="text-white hover:text-red-200 transition font-medium">Signup</a>
      <% } else { %>
        <form action="/logout" method="POST" class="inline">
          <button type="submit" class="text-white hover:text-red-200 transition font-medium">Logout</button>
        </form>
      <% } %>
    </div>
  </div>
</nav>
